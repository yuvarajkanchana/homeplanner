# Leave empty for local Vite proxy, or set to your deployed backend URL.
VITE_API_URL=

# Used only by Vite's dev server proxy. Docker Compose overrides this.
VITE_DEV_API_PROXY=http://localhost:8000
